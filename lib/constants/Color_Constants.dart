import 'package:flutter/material.dart';

class Colors_Constants{
  static const primary = Colors.deepPurple;
  static const app_bg = Colors.white;
  static const btnColor = Colors.blue;
  static var tileColor = Colors.deepPurple.withOpacity(0.5);
}